{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ReframeFromPerspective",
  "type": "object",
  "description": "Reframes a given text from a specific perspective, such as a role, stakeholder, or mindset.",
  "properties": {
    "perspective": {
      "type": "string",
      "description": "The perspective from which to reframe (e.g., a role, stakeholder, or mindset)."
    },
    "reframed_text": {
      "type": "string",
      "description": "The content as reframed from the given perspective."
    }
  },
  "required": ["perspective", "reframed_text"]
}
