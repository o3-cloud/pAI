{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Rating",
  "type": "object",
  "description": "A numeric rating extracted from a text, typically on a scale from 1 to 5.",
  "properties": {
    "rating": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "description": "A numeric rating, typically on a scale from 1 (worst) to 5 (best)."
    }
  },
  "required": ["rating"]
}